import { createTheme, Theme } from "@mui/material";

export const theme: Theme = createTheme({
  palette: {
    background: {
      default: "#eef0f1" 
    },
    primary: {
      main: "#000",
    },
    secondary: {
      main: "#f04e4a",
    },
  },
});
